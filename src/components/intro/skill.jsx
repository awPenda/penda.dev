const Skill = (props) => {
    return(
        <div id="Skill">
            <h2>{props.name}</h2>
            <div>{props.details}</div>
            

            {/*call skill componenet with variable to change*/}
        </div>
    )
}

export default Skill

